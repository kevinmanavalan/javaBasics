package LeetCode;

public class SeatingArrangement {
//    exports.SeatingStudents = function (arr) {
//  const [noOfDesks, ...occupiedDesks] = arr;
//        let count = 0;
//  const isOccupied = (j) => occupiedDesks.indexOf(j) != -1;
//  const isInRange = (k) => k <= noOfDesks;
//        for (let i = 1; i < noOfDesks - 1; i++) {
//            if (isOccupied(i)) continue;
//            if (i % 2 == 1) {
//                if (!isOccupied(i + 1)) count++;
//            }
//            if (isInRange(i + 2) && !isOccupied(i + 2)) count++;
//        }
//        return count;
//    };
}
